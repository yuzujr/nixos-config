# Per-user nix config

`~/.config/nix/nix.conf` can be provided via agenix when needed:

- `/etc/agenix/nix-user.conf`

Add `nix-user.conf.age` to your private `nix-secrets` repo if you need per-user access tokens.
