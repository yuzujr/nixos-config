# drcom-client-cpp

`~/.config/drcom-client-cpp/drcom_jlu.conf` is provided by agenix when present:

- `/etc/agenix/drcom-jlu.conf`

Add `drcom-jlu.conf.age` to your private `nix-secrets` repo to enable it.
